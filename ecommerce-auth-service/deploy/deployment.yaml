apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: puje2docker/auth-service:latest
          ports:
            - containerPort: 8080
  selector:
    matchLabels:
      app: auth-service
