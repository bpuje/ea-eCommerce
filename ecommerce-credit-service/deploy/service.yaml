apiVersion: apps/v1
kind: Service
metadata:
  name: deployment-service
  labels:
    app: deployment-service
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: deployment-service
